
SVG.on(document, 'DOMContentLoaded', function() {
    // var draw = SVG().addTo('body')
    // // var draw = SVG('drawing').size(300, 130)
    // var rect = draw.rect(100, 100).fill('#f06').move(20, 20)
    // var rect = draw.rect(100, 100).fill('#f06').move(200, 20)
    var draw = SVG('drawing').size(300, 130)

var rect = draw.rect(100, 100).fill('#f06').move(20, 20)
  })